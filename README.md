# design.ericbs.dev
Design components

The component library ships as [`@ericbs-ai/react-glass-components`](packages/react-glass-components): `bun run build:pkg` to build, then `npm publish` from `packages/react-glass-components`.
